// Este método me dice si un elemento esta o no dentro del array,devuelve true o false según corresponda.

const pets = ['cat', 'dog', 'bat'];

const rta = pets.includes(item => item === 'dog')
console.log(rta);